import { MockPage } from "@/components/MockPage";

export default function ProductPage() {
  return (
    <MockPage
      eyebrow="Product"
      title="Compute, reimagined."
      description="A closer look at the platform is on the way. For now, explore the network or review the investment opportunity."
    />
  );
}
