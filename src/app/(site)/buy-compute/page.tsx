import { MockPage } from "@/components/MockPage";

export default function BuyComputePage() {
  return (
    <MockPage
      eyebrow="Buy compute"
      title="Scalable compute on demand."
      description="Purchasing flows are being built. Soon you'll be able to provision compute directly from the grid."
    />
  );
}
