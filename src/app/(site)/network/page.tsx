import { MockPage } from "@/components/MockPage";

export default function NetworkPage() {
  return (
    <MockPage
      eyebrow="Network"
      title="A grid of distributed compute."
      description="Details on the network topology and participating sites are coming soon."
    />
  );
}
