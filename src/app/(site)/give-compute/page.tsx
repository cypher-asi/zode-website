import { MockPage } from "@/components/MockPage";

export default function GiveComputePage() {
  return (
    <MockPage
      eyebrow="Give compute"
      title="Put idle hardware to work."
      description="Onboarding for compute providers is in progress. Check back soon to contribute capacity to the grid."
    />
  );
}
